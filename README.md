# aws_task2

Add around 2000-2500 objects to S3 with tags and metadata (for e.g. Natural numbers). Fetch all objects, delete the ones with specific tags and metadata. All operations to be done using S3 boto3 api.Also write unit tests for all functions.(Brownie points if you can try different mocking libraries efficiently.)